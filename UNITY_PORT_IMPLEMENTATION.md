# Integrated Unity port — implementation and acceptance record

Last verified: 2026-09-13 (original integration September10). Project: `C:\Users\kelli\Gamesim Big Brother`, Unity 6000.6.0f1. The local six-person house/season is playable; the original game's complete feature set and AAA production acceptance are **not** claimed complete.

## Material changes from the preserved U02 baseline

| Area | Files | Change |
| --- | --- | --- |
| Source-control hygiene | `.gitignore`, `.gitattributes` | Ignore local environment credentials; preserve example files; mark the new baked NavMesh as binary. Generated builds, logs, caches and user settings remain ignored. |
| Pure simulation assembly | `Assets/Gamesim/Simulation/Gamesim.Simulation.asmdef` | New engine-independent C# assembly with no UnityEngine references. |
| State and authority | `Simulation/EpisodeState.cs`, `EpisodeEngine.cs`, `EpisodeValidation.cs` | Stable six-cast identity, detached snapshots, directed relationships, private knowledge, alliances, promises, counters, phases, seeded state, bounded receipts/history, atomic rejected commands and duplicate protection. Regular weeks, final-three competitions, final eviction and jury winner are implemented. |
| Source-verified rules | `Simulation/SeededRandom.cs`, `WebRules.cs`, `WebEvictionVoting.cs`, `WebEnduranceCompetition.cs` | JavaScript-compatible seed/hash/rounding arithmetic, relationship/promise/competition helpers, complete ten-factor per-voter evaluation and survival-elimination endurance. Source-generated fixtures document exact boundaries. |
| Content | `Simulation/ContentCatalog.cs` | Player plus Maya Hassan, Taylor Kim, Jamie Roberts, Casey Wilson and Riley Johnson. Source identities/traits, fixed scenario stats, authored motives/rooms/directed initial relationships and owned arrival memories. |
| Unity adapter/UI | `Runtime/Episode/EpisodeDirector.cs`, `EpisodeHud.cs` | Nearby line-of-sight conversation, real room-station gating, social/ceremony decisions, precision challenge with untimed assisted alternative, journal, local save/recovery/import UI, private-information filtering, keyboard focus restoration/scrolling and large-text/mute/reduced-motion settings. No per-frame deep state cloning; nearby sight checks reuse a buffer. |
| Save safety | `Runtime/Persistence/EpisodeSaveStore.cs`, `EpisodeSaveValidation.cs`, `WebSaveImporter.cs` | Strict versioned/checksummed JSON, finite/structural validation, durable verified temporary writes, atomic backup rotation, explicit recovery with damaged originals retained, safe new-slot staging and source archive before restricted web import. Unknown/rich unsupported saves are rejected rather than flattened. |
| Native presentation | `Runtime/Presentation/CharacterPresentation.cs`, `HouseAudio.cs` | Six distinctive generated articulated characters, walking/talking animation, owned material lifecycle, generated ambience/action cues and captioned outcomes. These remain stylized procedural assets, not final external art/voice or full ceremony choreography. |
| Camera/entry | `Runtime/House/HouseCameraRig.cs`, `Runtime/Bootstrap/GamesimBootstrap.cs` | Modal camera input lock, reduced-motion path without automatic dialogue reframing, and Bootstrap preference for EpisodeHouse with the old house fallback. |
| New scene/data | `Scenes/EpisodeHouse.unity`, `Data/EpisodeHouseNavMesh.asset` | Separate copied house scene with six cast slots, native episode wiring, and unique baked navigation data. Original scene and NavMesh are not overwritten. |
| Editor tooling | `Editor/EpisodeProjectSetup.cs`, `PortPreview.cs`, `U01ProjectSetup.cs` | Create-if-absent guarded episode registration; unique NavMesh and isolated spawn validation; save-isolated preview with scene restoration; separate clean-cache Windows build, desktop defaults and original input preload preservation. |
| Assembly dependencies | Runtime/Editor/EditMode/PlayMode `.asmdef` files | Simulation references; Edit Mode NUnit/Newtonsoft precompiled references. |
| Package manifest/lock | `Packages/manifest.json`, `packages-lock.json` | Explicitly pin Newtonsoft JSON 3.2.2, which was already resolved transitively; no Unity/rendering/input package upgrade or removal. |
| Project settings | `ProjectSettings/EditorBuildSettings.asset`, `ProjectSettings.asset` | Append EpisodeHouse after the three retained build scenes. Desktop build uses a resizable 1600×900 window and retains the original Input Actions preload. |
| Regression coverage | `Tests/EditMode/EpisodeEngineTests.cs`, `EpisodeRecoveryBoundaryTests.cs`, `PersistenceTests.cs`, `WebParityTests.cs`, `WebVotingParityTests.cs`, `WebEnduranceParityTests.cs`; Play Mode episode/accessibility files | Full-seed seasons, rejection/retry/reload integrity, private/public voting explanations, final-stage boundaries, recovery/corruption and real scene/UI/navigation tests. Existing baseline tests remain; the Bootstrap assertion now expects EpisodeHouse and uses isolated saves. |
| Fixture provenance | `Tests/EditMode/Fixtures/` | Original-source SHA-256 manifests and arithmetic/voting/endurance golden JSON plus scope README. The web reference is a local source capture, not a Git checkout with an invented commit ID. |
| Opt-in QA | `Runtime/Episode/PortVerification.cs` | Inert unless launched with explicit verification and isolated save-root arguments. Records graphical workload timings, available allocation counters, resolution screenshots, hardware and errors; never changes the normal active save slot. |
| Documentation | `UNITY_PORT_ROADMAP.md`, this file | Updated milestone evidence and explicitly open production/full-parity work. Historical U02 report retained. |

Every new Unity asset/source has its Unity-generated `.meta`. No commit, remote assignment, push, PR, backend write or credential change was performed in this local project.

### Schema-2 continuation

The next native increment adds these material changes without overwriting the pinned schema-1 player:

- `Simulation/EpisodeState.cs`, `EpisodeFinale.cs`, `EpisodeFinaleValidation.cs`, engine/validation: append jury-questioning and final-speech phases without changing old phase ordinals; persist each prepared question/answer and generated speech; bound and validate all records; add mood/stress, directed relationship notes/events and relationship arcs. Existing command receipts and clone-before-commit authority apply to the new commands.
- `WebJuryQuestioning.cs`/catalog, `WebFinalSpeeches.cs`/catalog, `WebRelationshipArcs.cs`: original-source offline questioning/response, final-speech and arc helpers with source-generated goldens. Player-finalist answers apply ±10 direct trust with distinct reciprocal-event and reciprocal-score draws. Player-juror questions use the source one-draw offline reply without a trust bonus. Final speeches do not change jury scores. Native speech context filters to the speaker's own alliance memberships, avoiding the original UI's unrelated private-alliance disclosure.
- Current-source nomination correction: initial nominees receive the source mood/stress changes, and player-involved nominations apply −8 to the relationship arc, not a spurious normal-trust penalty. Replacement nomination does not apply initial mental effects. Veto relationship changes now carry source notes and arc effects. Historical schema-1 scores are retained, not recalculated. This does not yet implement separate loyalty oaths or modern deal consequences.
- `WebEvictionVoting.FromNative` now supplies the actual stored arcs. They are not fabricated into storylines/deals. Relationship event time uses bounded logical sequence IDs instead of source epoch timestamps; retention limits are native policies. Full decay/milestone systems remain separate work.
- `HouseDialogue.cs`, `EpisodeDirector.cs`, `EpisodeHud.cs`: five authored voices react only to direct known promises, shared alliances, owned memories and their own feelings; no exact NPC trust-score disclosure. Finale panels support answering/asking, skipping remaining questions, persistent speech drafts, 2,000-character submission/skip, keyboard Tab/Escape, and large-text wrapping. Notebook includes mood/stress and arc narrative. Rich text stays disabled for authored/imported speech.
- `EpisodeSaveMigrations.cs`, store and storage validation: frozen recursive schema-1 contracts; validate original checksum before a detached in-memory v2 upgrade. New fields receive explicit defaults, scores/RNG/receipts/old Jury/Finished progress stay unchanged. Load does not rewrite files. The next save rotates the original v1 bytes into backup; recovery also preserves those bytes and the damaged primary. No generalized web-save migration is claimed.
- New Edit Mode dialogue/finale/migration and jury/arc/speech fixture tests; expanded Play Mode actual-button/full-season and jury/speech/reload/keyboard cases. The arc fixture loader uses Newtonsoft to preserve JSON nulls (Unity JsonUtility had materialized null as an empty expected event).
- `U01ProjectSetup.cs` now builds into `Builds/Port-Windows-V2`, reporting to `Logs/Port-v2-build-report.json`. Task verification script accepts an explicit player path and requires a zero process exit code; previous `Port-Windows` and `U02-Windows` outputs remain intact.

An isolated managed test harness used the installed .NET SDK. Its first invocation initialized the SDK cache and reported creation of an ASP.NET Core HTTPS development certificate; no trust command was run. Subsequent harness commands disabled first-run certificate generation and telemetry. This was an SDK side effect, not a game/backend credential setup.

### Schema-3 continuation (acceptance in progress)

- `EpisodeDirector.DiaryRoom.cs`, Director/HUD and diary Play Mode tests: make the existing Private room marker usable without modifying any geometry, scene or navigation asset. R travels to the Diary Room (D remains camera pan); nearby E enters after proximity/line-of-sight checks. Shared player-decision rendering supports eligible nominations, veto decisions and private eviction voting. Drafts require confirmation; cancel/close is non-mutating. The diary shows only owned memories/direct commitments and never grants ceremony authority or advances a phase itself.
- `EpisodeSocialHistory.cs`/validation and state schema3: persist post-eviction diary invitation/skip/choice, persona history, cumulative source bonus counters, the separate jury-impression ledger, structured player loyalty declarations and once-per-NPC 75-crossing opportunities. Source pure diary/persona, sentiment and oath helpers are separately fixture-tested. Reflect/skip commands are stale-safe and receipt-protected; pending reflections cannot be silently overwritten by the next week. Source midweek/nomination prompt generators are present as pure helpers but are not scheduled until the corresponding activity scheduler exists.
- Native diary display uses neutral prose instead of the original catalogue's unverified claims that an evictee trusted the player or that the player orchestrated the eviction. It also does not claim a seated/recording animation that is absent. Choice identity/persona/effective source effects remain intact. Source-declared reputation/stealth rewards are not applied; the original inspected handler does not dispatch them. Social/competition counters retain the original cumulative storage behavior but are not advertised as bonuses in normal native gameplay, which does not invent a consumer. Persona feeds the existing source voting evaluator; jury impressions remain separate from actual directed trust and ballots.
- Loyalty declarations are the player's own commitment, not a guaranteed NPC acceptance or reciprocal promise. A positive player relationship crossing75 offers the action once, claimable in social time. Declaration uses source one-way +5, last-interaction week and latest20 notes without RNG/event/arc side effects; timestamps are native logical sequence IDs. Nomination oath breaches use their distinct source arc/ripple calculation before normal nomination effects. Vote oath and Vote-promise consequences are deliberately deferred until the public eviction reveal so private ballots cannot leak via breach logs, trust, memories or RNG changes, or change this round's later voters. Four dedicated ballot-privacy regressions cover kept/broken promises, reload and duplicate reveal. Source arithmetic is retained; this timing is an explicit privacy adapter. Historical v2 pending ballots retain any already-recorded consequences instead of undoing/replaying history. Campaign dialogue's separate loyalty-proposal reaction is leaf-ported but is not mislabeled as a stored oath.
- Persistence now validates frozen recursive v1 and v2 contracts before adding explicit v3 defaults. It does not replay history or retroactively add jury impressions, persona choices or oaths. `ObjectCreationHandling.Replace` prevents saved persona-score collections from being appended to constructor defaults during load. Long integer shape checking supports logical oath timestamps. New migration/round-trip tests protect nonempty records and both old backup formats.
- New source fixtures and Edit/Play Mode coverage are being added for all these boundaries. Current build target is `Builds/Port-Windows-V3/Gamesim.exe`, report `Logs/Port-v3-build-report.json`; previously accepted binaries remain intact. No v3 runtime/build acceptance is claimed before its results are recorded.

## Preservation

Before this work, the project content/settings were copied to the task's `work/unity-u02-backup-20260910`. The earlier U01 backup also remains. A hash comparison against the U02 backup found no missing original files. Original `Bootstrap.unity`, `HousePrototype.unity`, `HousePrototypeNavMesh.asset`, `SampleScene.unity`, prototype materials and other original assets were unchanged. The runtime bootstrap script now selects the new episode. Existing `Builds/U02-Windows` was not replaced; the new build uses `Builds/Port-Windows`.

The first new-scene attempt failed safely during NavMesh validation. Only its newly created destination assets were rolled back; the working original remained. The corrected generator copies unique navigation data and validates it in an isolated offset NavMesh instance before registration.

## Verification

- Edit Mode: **62 passed, 0 failed, 0 skipped**, run `5343549331a845c29af23687020f1090`, 2026-09-10 23:15:18–23:15:25 UTC. `Logs/U01-editmode-results.txt` and XML. Includes 100 seeded complete seasons, independent source fixtures and persistence/recovery validation.
- Play Mode baseline: **20 passed, 0 failed, 0 skipped**, run `ebf371ddb47746eaa5e463c514ddea42`, 23:30:04–23:31:03 UTC. `Logs/U02-playmode-results.txt` and XML. Includes actual station path traversal and ceremony buttons through a saved finale, five-room reachability, promise-button, reload, corruption/recovery, keyboard/scroll/large-text and reduced-camera-motion tests. Automated navigation is accelerated; it is not a human pacing test.
- The first integrated Play Mode run exposed a scene-unload MissingReferenceException. The director now tolerates destroyed siblings and does not rebuild UI during teardown; the two subsequent full suites passed.
- An isolated Editor preview rendered the house and Settings UI, then restored the original clean HousePrototype scene. Captures are retained under `Logs/Port-preview-df5e870292f048718f5f105e6a063ebb`. Editor captures use the small existing Game view, not the final desktop resolution.
- Schema-v1 baseline Windows build succeeded with strict/clean-cache options: 0 errors, 510 existing package warnings, no Gamesim-owned warnings; 143,215,759 bytes. Report: `Logs/Port-build-report.json`. Original preloaded Input Actions retained. The two project-owned obsolete lookup warnings from the earlier build were fixed.
- Five-minute graphical baseline workload **Passed**, 23:39:04 UTC, GTX 1060 6GB / i5-8400, 1600×900: 215,904 frames, median 1.20 ms / p95 2.48 ms / p99 3.23 ms; no runtime errors. The allocation recorder was unavailable, so zero allocations are NOT claimed. These are workload frame intervals, not GPU timings, human gameplay or broader hardware acceptance. Report/screenshots/full-build hashes: `Logs/Port-verify-20260910-233348-42a93a449de044f192ec3f7d6e0b6620`. The graphical log includes unused stripped post-effect shader warnings.
- The first graphical workload correctly failed on kitchen/bedroom routes obstructed by NPCs. Only EpisodeHouse and its create-if-absent generator markers moved: kitchen `(3,0,-6)`, bedroom `(-5,0,2)`; original HousePrototype remains unchanged. New navigation regression passes. Startup captures now wait for splash completion and a rendered backbuffer; standard and large-text images at 1600×900, 1280×720 and 1280×800 were retained.
- The preceding results certify the retained schema-v1 baseline only. Schema-v2 acceptance is separately recorded below.
- Schema-2 Edit Mode: **132 passed, 0 failed, 0 skipped**, run `11f6fc42074043fdb42c345fe24ccf8f`, 23:54:43–23:54:54 UTC. Retained `Logs/Port-v2-editmode-results.xml`.
- Schema-2 Play Mode: **24 passed, 0 failed, 0 skipped**, run `7a3fac8cf8e04a1b96a4884f5853a066`, 23:55:22–23:56:49 UTC. Retained `Logs/Port-v2-playmode-results.xml`; includes real jury buttons, role-specific questions, retained multiline speech drafts through Tab/Escape/rebuild, submitted text reload, explicit skip and complete season.
- Schema-2 strict clean-cache build: **Succeeded**, 0 errors, same 510 package warnings, 143,302,031 bytes, 89.14 seconds. Retained `Builds/Port-Windows-V2/Gamesim.exe` and `Logs/Port-v2-build-report.json`.
- Schema-2 standalone graphical workload: **Passed**, exit 0, 2026-09-11 00:04:45 UTC, 300.03 seconds / 210,936 frames, median 1.22 ms / p95 2.53 ms / p99 3.32 ms, GTX1060/i5-8400 at 1600×900, no runtime errors. Allocation counter unavailable; no zero-allocation claim. Full-build hashes and screenshots: `Logs/Port-verify-20260910-235930-0c4173248aee4482ab8b33404b9fa4a1`. Large-text 1280×720 layout visually inspected. These timings describe the automated workload, not human pacing or an isolated hardware benchmark.
- After tests/build, read-only MCP reported the original `HousePrototype` active, loaded, not dirty, with 12 roots; Console error query returned none. A separate source/settings snapshot is retained in task `work/unity-v2-source-20260910`.
- Schema-3 Edit Mode: **275 passed, 0 failed, 0 skipped**, run `4d7d77ce3c654263851d784e63caff12`, 2026-09-11 00:23:34–00:24:04 UTC. Retained `Logs/Port-v3-editmode-results.xml` and `.txt`. Includes source diary/persona/sentiment/oath parity, integrated engine and ballot privacy cases, and frozen v1/v2 migration with nonempty v3 round-trip/recovery.
- First schema-3 Play Mode run: 37 passed / 2 failed. Both oath-button fixtures incorrectly expected 18 talks at +4 to reach the 75 milestone (they reach 72). The fixture now uses the actual alliance-proposal button once reciprocal trust is eligible, then continues legal conversations within the same 18-action budget. No balance or engine rule was changed. Failed evidence retained in `Logs/Port-v3-playmode-first-failed.xml` and `.txt`; full-suite rerun required.
- Later schema-3 Play Mode report recovered on resume: **39 passed, 0 failed, 0 skipped**, run `87a86feb9c7845b18aba9714702dbd2a`, 2026-09-11 02:30:01–02:33:34 UTC, 210.24 seconds. Retained as `Logs/Port-v3-playmode-20260911.xml` and `.txt`. This closes the two oath-fixture failures above; a fresh September13 runtime run is being checked separately.
- September13 read-only reconnect: Unity idle; `HousePrototype` active, loaded and clean, now with 17 roots; Console error query empty. The live scene and ten existing prototype materials differ from the previously retained v2 snapshot, with additional plant materials. These later local changes were preserved, not reverted or attributed to the current acceptance work. Missing old `Assets/Resources/PerformanceTestRun*.json` and their folder metadata are transient Unity test-run resources. New pre-work source/settings snapshot: task `work/unity-resume-source-20260913` (328 files, 4,630,558 bytes). Earlier preservation statements describe their original inspection time, not an assertion that subsequent user/project edits never occurred.
- September13 fresh Edit Mode: **275 passed, 0 failed, 0 skipped**, run `8451f440b4e04ac39076e264da6e1abe`, 16:04:42–16:05:07 UTC, 21.11 seconds. Retained `Logs/Port-v3-editmode-20260913.xml` and `.txt`.
- September13 first Play invocation discovered zero tests and was correctly rejected by the nonzero-pass acceptance gate despite NUnit's empty `Passed` result. Retained `Logs/Port-v3-playmode-empty-20260913.xml`/`.txt`. Added guarded `Editor/PortTestDiscovery.cs` menus to inspect loaded/compiled/discovered test assemblies and request a script reload when idle, without modifying packages or scenes. After the compile/domain reload, `Logs/Port-test-discovery.json` identified all 39 Play Mode tests. This is consistent with stale discovery; the exact original cache cause was not proven. A real nonempty execution is still required.
- The domain reload also emitted Unity Editor `MainToolbarWindow` layout-stabilization errors from UnityEditor/UIElements, not Gamesim runtime code. These were not suppressed or cleared to claim a clean Console. Standalone runtime and later test results are reported separately.
- September13 nonempty Play Mode retry: **39 passed, 0 failed, 0 skipped**, run `ec0a743c06fa40cf9c04a2ee84ced4a6`, 16:11:24–16:15:14 UTC, 224.93 seconds. Retained `Logs/Port-v3-playmode-20260913.xml` and `.txt`. Read-only Console query during this real run returned no errors. This validates the live schema3 runtime before the additional opt-in standalone-season verifier is integrated.
- Added the opt-in `Runtime/Episode/PortVerification.Season.cs` partial and small hooks in `PortVerification.cs`. Only explicit `--gamesim-verify --gamesim-verify-season --gamesim-save-root <absolute QA folder>` runs use it; ordinary startup remains unchanged. It creates a new isolated slot via actual UI, legally earns an oath through conversations/alliance when available, walks to stations/Diary Room, uses actual buttons through a complete season, checks confirmation/reload and prior-slot byte preservation, and captures the reached finale role. It does not fabricate state or claim both mutually exclusive role branches passed. Its ten-minute functional deadline/report is separate from the five-minute profile samples. The task launcher now accepts `-VerifySeason`, requires both reports and a zero exit code, and extends its deadline only when requested.
- Hardened `Editor/PortTestDiscovery.cs` after independent source review: observed all-caller execution tracking through public callbacks and SessionState, callback exception containment, 60-second watchdog, late-callback protection, and refresh-before-retry after timeout. The installed framework's callbacks do not prove the scheduled/prebuild/cleanup interval is idle; externally verified idle/clean-scene checks remain required. The staged helper metadata-compiled with zero warnings/errors; actual Unity discovery is checked separately. No framework/package code was changed.
- Final source regression after the QA integration: **275 Edit Mode + 39 Play Mode passed, zero failed/skipped**. Edit run `151990ec05074a1690809de7c23f7c83`, September13 16:19:46–16:20:09 UTC, 19.40 seconds; Play run `0cab53571b7d4328b58b74b7470c8ae5`, 16:20:44–16:24:19 UTC, 209.46 seconds. Retained `Logs/Port-v3-final-editmode-20260913.xml`/`.txt` and `Logs/Port-v3-final-playmode-20260913.xml`/`.txt`. New diagnostic report also discovered all39 tests. These passes do not substitute for executing the standalone-only verifier.
- **Schema-3 desktop automated acceptance now passes within the documented native scope; final evidence follows. Earlier v1/v2 executables and snapshots stay preserved. This is not complete-original-game, human-pacing or AAA visual acceptance.**

### September13 standalone acceptance follow-through

- Initial strict/clean-cache v3 desktop build succeeded: 0 errors, 505 warnings, 143,974,979 bytes, 172.53 seconds. Eight warnings belonged to nullable fields on transient Diary Room helper DTOs, rather than package code. They were resolved before the final pin; the first report remains `Logs/Port-v3-build-first-20260913.json`, not relabeled as warning-free.
- Two isolated headless functional runs passed with exit0. Reports are under `Logs/Port-verify-20260913-163127-7d69c7212b604999800cbf527573a4cf` and `Logs/Port-verify-20260913-163254-221e1275e9ac4c24ac5f8974907c7e9c`. Each completed54 authoritative decisions, four save/reload checkpoints, a legally earned and declared oath, and a player-juror finale while preserving the profile's prior save bytes. Seeds differ (`2221984243`, `2964338462`); no state or role was fabricated. The player was evicted before post-eviction reflections became available in both runs, so neither certifies reflection or player-finalist standalone coverage. Headless frame intervals are not graphical performance evidence.
- An independent read-only review found no actionable verifier routing defects in player/NPC veto decisions, tied ballots, finalist/juror questioning, speeches or save/reload transitions. The runtime reports remain the execution evidence, not that review alone.
- Corrected the eight project-owned serialization warnings by removing `[Serializable]` from four transient catalog/choice-plan DTOs in `Simulation/WebDiaryRoom.cs`. Applied the same accurate contract to six JSON-only nested fixture DTOs in `Tests/EditMode/WebDiaryParityTests.cs`. Persisted persona/state types retain their annotations; no fields, gameplay rules, schema, dependencies or JSON members changed. Plain `[NonSerialized]` was deliberately not used because it would omit nullable fields from default Json.NET fixture contracts. A managed check using the installed Unity analyzer reports zero warnings/errors; diary fixtures, existing regressions, and null/0/-5/+7 round trips pass. Complete normalized fixture JSON hash stays `0F553D045D05017E1F2FA339ED8B4CE05BDC80F3458CFCFDC170777D9628722F` before and after. Full Unity Edit Mode and desktop acceptance are checked separately.
- Post-warning-fix Unity Edit Mode: **275 passed, zero failed/skipped**, run `437b3602733c47f1916ad996aa554805`, 16:38:05–16:38:30 UTC, 21.47 seconds. Retained `Logs/Port-v3-warningfix-editmode-20260913.xml`/`.txt`. The preceding39-pass Play Mode run tested identical gameplay logic before the annotation-only fix; the rebuilt standalone below validates the final runtime.
- Final strict/clean-cache v3 build: **Succeeded, 0 errors, 497 package warnings, no Gamesim-owned warnings**, 143,974,979 bytes, 77.32 seconds; report written16:40:09 UTC. `Builds/Port-Windows-V3/Gamesim.exe`, `Logs/Port-v3-build-report.json`.
- Final graphical run **Passed, exit0**: `Logs/Port-verify-20260913-164115-4093d263ac3c40c8bae5774e027c8666`. Profile ended16:46:29 UTC after300.03 seconds/251,327 frames at1600×900, GTX1060 6GB/i5-8400: median1.10ms, p95 1.78ms, p99 2.32ms; runtime errors empty. Allocation recorder unavailable, so zero allocation is not asserted. Frame intervals describe this automated workload, not isolated GPU timing, human pacing or broader target-hardware coverage.
- The same final executable then passed a separately timed112.21-second functional season, seed`2400448639`, ending16:48:21 UTC: **62 decisions, three Diary Room reflections, eight save/reload checkpoints, a legally earned/declared oath, four player-finalist answers, typed/submitted/reloaded final speech, and completed RunnerUp finale**. The earlier profile slot's bytes were preserved. Both reports pass and the process exits0; `build-pin.json` hashes all215 build files. Player-juror questions were not reached in this graphical seed (they passed in the two earlier headless runs); precision input/human pacing are outside this runner. No role/state was fabricated.
- Visually inspected house startup, large-text1280×720 Settings, Diary Room choices/review, speech field and finale captures. Controls/text are readable with intentional scrolling. One cosmetic limitation remains in accepted v3: the finished phase subtitle says “0 houseguests remain” because winner/runner-up are no longer Active; the next staged UI increment corrects that wording without changing result statuses.
- Final read-only MCP: `HousePrototype` loaded/clean with17 roots; Editor idle/not compiling; latest Console error query returns0. Earlier editor-toolbar errors remain documented above, not claimed fixed. Hash comparison against today's pre-work snapshot shows no scene, material, NavMesh, package or ProjectSettings changes. All113 non-meta Gamesim files have metadata (71C# files).
- Today's exact live Assets delta: added `Editor/PortTestDiscovery.cs` and `Runtime/Episode/PortVerification.Season.cs` with their metadata; modified `Runtime/Episode/PortVerification.cs`, `Simulation/WebDiaryRoom.cs`, `Tests/EditMode/WebDiaryParityTests.cs`, and `Tests/EditMode/Fixtures/README.md`. Also updated these project roadmap/evidence documents and task `work/unity-port-verify.ps1`; generated reports/captures/QA saves/build output stay ignored. No files were deleted, no normal player saves were touched, and no Git commit, remote, PR, credentials or backend configuration was added.
- The existing scheduled continuation was updated to follow this Unity plan and evidence rather than the obsolete web PR10 queue; its schedule was retained. Study and voting-bloc helpers plus the NPC-autonomy audit remain staged outside live Assets until their separate integration gates pass.
- Immutable v3 source/settings snapshot: task `work/unity-v3-source-20260913` (333files, 4,694,403bytes). Task `work/unity-v3-acceptance-20260913.json` indexes the final reports and scope. All215 executable-build file hashes were rechecked with zero mismatches before beginning the next increment.

## Rule/persistence boundaries

### Schema4 study increment — integrated and automated desktop acceptance passed

The accepted schema3 executable and source snapshot above remain immutable. The following new source is not certified by that earlier evidence.

- Added `Simulation/EpisodePreparation.cs` and `WebStudyHouse.cs`; appended StudyHouse=20 and SimulateCompetition=21 commands. Study is an explicit active-player Social action from the private room, costing one of18 actions, with exactly one saved RNG draw on confirmation. Memorize grants+1; sneaking is45% success/+2 or failure/−1, clamped0–5. No trust, arcs, memories, persona, mood, jury ledger or ballots change. The native cast does not fabricate the distinct source personalityTraits field or its Strategic modifier. Private-room/Social/budget locks are native authority adapters, not claims of original UI timing/budget parity.
- Added persisted integer `playerStudyBonus`, default0, schema4 validation, frozen recursive `Runtime/Persistence/FrozenEpisodeV3.cs`, and explicit v1/v2→v3→v4 migration. Original payload checksums and historical shapes/enums are checked first. Existing saves are not rewritten on load. The new simulated-score ceiling is1030 to accommodate previously valid event counters; old schemas still reject scores above their original1000 ceiling. Existing recovery/import boundaries remain unchanged; unsupported nonzero preparation in web imports is still rejected.
- Added a separate weekly HoH/Veto simulated option using stored event+study input in the source weighted scoring leaf. Preparation persists across weeks and is not spent. Normal precision/accessibility, NPC-only advances and all finale routes retain their previous arithmetic; the new mode is rejected outside eligible unresolved weekly competitions. The native category schedule/cast/persisted RNG are explicit scenario adapters, not original full fast-forward replay equivalence.
- Updated `EpisodeDirector.cs`, `EpisodeDirector.DiaryRoom.cs` and shared HUD captions with readable review/confirm/cancel and own preparation display. Old detached buttons cannot confirm or cancel a replacement draft. Finished subtitle now says “Season complete” rather than counting non-active finalists as zero remaining. No winner/status rule changed.
- Added preparation, frozen-v3 migration and source study Edit Mode tests plus real-button Play Mode tests. Updated existing migration assertions for current schema4 while preserving literal historical fixtures. Appended fixture provenance; copied the generated1648-case source JSON unchanged with SHA256 `240df7fd1b4b94bfffbe3a5f72f8684ca0add6fb85e2a62c208d38cf8db3ee95`. Managed overlay:323 checks passed, zero failed; this is not Unity UI/test/build acceptance.
- `Editor/U01ProjectSetup.cs` now builds to distinct `Builds/Port-Windows-V4/Gamesim.exe` and `Logs/Port-v4-build-report.json`. No earlier build was overwritten. Before integration, all12 modified-file baselines matched, all8 new targets were absent and all staged hashes matched. No scene, material, NavMesh, package or normal save changes were included.

Source and staged integration detail: task `work/unity-port-next/study-integration/INTEGRATION.md`. Actual acceptance evidence follows; the earlier managed result is not used in place of Unity execution.

### Schema4 execution evidence — September13

- First Unity Edit Mode: **326 passed, zero failed/skipped**, run `6b6cb72f55df4015806938628df4355f`, 17:01:29–17:02:02 UTC, 29.96 seconds. Retained `Logs/Port-v4-first-editmode-20260913.xml`/`.txt`.
- Unity Play Mode: **45 passed, zero failed/skipped**, run `97af65f98e5a41228045649ec0113c28`, 17:02:28–17:07:12 UTC, 278.68 seconds. Retained `Logs/Port-v4-first-playmode-20260913.xml`/`.txt`. Includes the new study review/confirmation/cancel, stale button, preparation persistence and simulated-competition UI coverage.
- Added standalone-only `Runtime/Episode/PortVerification.Study.cs` and hooks in both existing verifier partials. Explicit study verification requires the isolated save-root and full-season flags; normal gameplay and Editor tests do not run it. It uses actual UI controls for five memorize confirmations, cancellation/full-state and RNG checks, preparation reload, eligible weekly simulated competitions, and final-HoH exclusion. The mandatory study result fails the overall run and process on a missing assertion. No seed, role or season state is fabricated.
- Updated task `work/unity-port-verify.ps1` to default to the separate v4 executable, require the nested study report when requested (an old executable ignoring the flag cannot pass), and accept an existing schema3 QA save as an optional migration seed. It copies the original bytes only into a new isolated QA directory. The player validates the old checksum; the launcher compares the complete migrated state with the old state plus schema4/preparation0, and rechecks the external original's hash. Routine autosaves may rotate the QA backup, so preservation is established from the original outside that directory, not an assumed backup format. Normal save locations remain untouched.
- After the standalone-only QA additions, Unity Edit Mode again passed **326/0/0**, run `dbaa3f25c2a4422b86be7b60a606ecb0`, 17:09:27–17:10:00 UTC, 29.55 seconds; `Logs/Port-v4-final-editmode-20260913.xml`/`.txt`. The 45-pass Play Mode run preceded these verifier-only additions; final runtime execution is checked below.
- Strict clean-cache v4 Windows build: **Succeeded, 0 errors, 497 package warnings, no Gamesim-owned warnings**, 144,006,211 bytes, 71.97 seconds. Report written17:11:52 UTC: `Logs/Port-v4-build-report.json`, executable `Builds/Port-Windows-V4/Gamesim.exe`. The recurring UnityEditor MainToolbarWindow/UIElements layout errors were observed again after domain reload; they are not attributed to Gamesim or claimed fixed. No package code was changed or Console evidence cleared.
- Isolated headless v4 study/season/migration run **Passed, exit0**: `Logs/Port-verify-20260913-171241-a08cec276bbe46958fe9e6cbe52ccaab`, completed17:14:53 UTC. Five study confirmations, five eligible weekly simulations, preparation5 retained to the end, eight save/reload checks, three Diary Room reflections, 58 decisions and a completed player-juror season. Oath was not reached in this legal seed. The separate season took112.20 seconds; the ten-second headless profile is not graphical performance evidence. Both reports have empty runtime error arrays.
- That run also passed a real schema3→4 copy migration from the accepted v3 profile slot at `Logs/Port-verify-20260913-164115-4093d263ac3c40c8bae5774e027c8666/episode.json`. Original SHA256 `EE6D309809DEEE2D7817C8C0D716D108D60CA8EE4F66F9F9A85DCDE56ACA19B1` is unchanged; complete migrated state is equal apart from schema4 and explicit preparation0. Study sneak outcomes, confirmation at the existing cap, and precise roll-boundary/forged-input cases belong to deterministic Edit/Play tests, not this five-memorize standalone workload.
- Five-minute graphical v4 workload **Passed, exit0**, completed17:24:15 UTC: `Logs/Port-verify-20260913-171909-f09fde5191bf48b590e17e4159f6230a`. 300.03 seconds, 262,830 frames,1600×900 on GTX1060 6GB/i5-8400; median1.04ms, p95 1.78ms, p99 2.35ms. Runtime errors empty. Allocation counter unavailable; no zero-allocation claim. These are automated-workload frame intervals, not isolated GPU timing, human pacing or broad hardware certification.
- The same executable then passed a separate65.71-second actual-UI season, seed`3588281201`, completed17:25:21 UTC:55 decisions, five study confirmations, three weekly simulated competitions (one HoH/two Veto), preparation5 through three week transitions, one diary reflection, two player-juror questions and six save/reload checkpoints. Final-HoH simulation was absent at six checks; this evicted-player seed did not exercise assisted finale play, a player-finalist speech or an oath, and those branches are not certified by this run. Profile-slot bytes stayed unchanged. Both study/season and overall reports pass; all215 build-file hashes were rechecked with zero mismatches.
- Graphical run also passed the same complete-state schema3→4 migration comparison and original-source hash preservation. Inspected house, large-text1280×720 Settings, study choices/review, weekly simulation choice/result and finale captures. Text and controls are readable with intentional scrolling; finished subtitle correctly says “Season complete.” Minor inherited wording remains in the pinned v4 competition notification: “You wins ...”; a future source-only wording fix must not be attributed to this binary.
- Final read-only MCP: HousePrototype loaded/clean with17 roots, Editor idle/not compiling/updating, latest Console error query0. Earlier transient editor-toolbar errors remain documented, not claimed fixed. Independent v4-versus-pinned-v3 audit found nine new non-meta Assets files with nine metadata files,15 modified Assets files, zero deletions and no changes to old metadata. All122 non-meta Gamesim files have metadata (79C# files), with no blank/duplicate GUIDs. Scenes, materials, NavMesh, Packages, ProjectSettings and source-control configuration remain byte-identical to v3 and preserve later user prototype edits.
- Final v4 material inventory beyond the earlier integration: new standalone Study verifier, frozenV3 persistence contract, preparation engine/helper, three Edit test sources/source fixture and one Play test source. Modified build setup, Director/Diary/HUD, two prior verifier partials, migration/store/storage validation, engine/state/validation, two historical migration test sources and fixture provenance. These two project documents and the task launcher were updated. Reports/screenshots/QA saves/builds remain generated ignored output; no normal player saves, remote, PR, credential or backend setup changed.
- Immutable v4 source/settings snapshot: task `work/unity-v4-source-20260913`; acceptance index `work/unity-v4-acceptance-20260913.json`. The matching launcher is retained as `work/unity-v4-verify-20260913.ps1`. Further staged voting-bloc work is not part of this accepted binary or snapshot.

### Schema5 voting-bloc increment — accepted within native scope

The accepted schema4 source/build/launcher remain preserved separately. Its results do not certify this later increment.

- Added `Simulation/WebVotingBlocs.cs`, `WebNativeEvictionRound.cs` and `EpisodeVotingBlocs.cs`, with narrow regular-eviction hooks in `EpisodeEngine.cs`. The complete missing NPC batch is evaluated from one detached pre-consequence snapshot, including the player and already-voted eligible alliance members in coordination draw order. Only missing NPC ballots are committed. HoH tie-breaks receive no directive; final-three eviction and jury voting retain their prior callers. Plans/directives/factors/defection evidence stay ephemeral and private. Only the original default public explanation is saved and revealed, at the existing reveal boundary; pending ballots cannot settle promises/oaths or alter later votes.
- Added schema5 integer `blocRulesStartWeek`: new native seasons default1; every migrated native v1/v2/v3/v4 save and restricted web import activates at capturedWeek+1, including untouched Social/week1 saves. The marker survives clone/save/reload and is constrained to1..101 and no later than savedWeek+1. Before it, the old evaluator and supplied nominee order are preserved. After it, source caller cast-order evaluation applies even with no eligible alliances; ordering ties may intentionally differ. No old ballot, reason, result, receipt, RNG state or consequence is rewritten. Load/import status explains the exact activation week while preserving the validated-load prefix.
- Added frozen recursive `Runtime/Persistence/FrozenEpisodeV4.cs`, explicit historical numeric/collection bounds, original enum checks and a detached semantic/storage validation view. The former migration dispatcher remains as `PrepareV4Payload`; earlier transformations/checksum-before-migration behavior remain unchanged. The returned JSON adds only the marker and schema5 after original validation. Restricted importer scope is not broadened; unsupported preparation context/custom bloc markers remain rejected. No source file is rewritten on load.
- Source coordination matches first-overlap directive priority, complete eligible-cast draws, independent week/active/nominee-seeded stream, raw-score proxy trust, supplied-nominee target ordering and cast-ordered per-voter alternatives. Actual native pact membership is used; absent founder/stability/grudge data is not fabricated from memories, arcs, oaths or promises. This is not dynamic alliance/deal/grudge lifecycle implementation. Removed unnecessary Unity serialization annotations from transient JSON helper DTOs while retaining nullable stability values for Json.NET.
- Added two source artifacts and parity/integration tests: `WebBlocFixtures.json` (1,527,748bytes, SHA256 `e9224058a1f7bcd137c3ba14ba4e76cab1c77fd9f521d6787132fc2e378508e2`) and `WebLegalBlocWitness.json` (49,924bytes, SHA256 `e605cf2c81a7060c96cb59b0fc93aaae89e585101b31b98b252216ac800c63fb`). All11 original authority hashes match. The legal seed4 witness forms a Maya pact through three talks/proposal and ordinary competition/veto commands; Maya changes Taylor→Casey under source bloc pressure with the same nominee order held constant. Private pressure is excluded from public reasons. Both generated artifacts were copied unchanged and hash-checked.
- Added45 frozen-save migration cases and updated only current-version expectations in older migration/preparation tests. Added two real-button `EpisodeBlocPlayModeTests.cs` tests covering legally earned pact influence, human ballot independence, private diary cancel/confirm/replacement/stale callbacks, actual Settings reload, reveal, notebook privacy and duplicate receipt behavior. Core source sweep includes100 complete seeded seasons and old-week/new-week/tie-break/finale boundaries. Combined staged managed checks:555 passed, zero failed; staged UI metadata compilation:zero errors/one cross-target Newtonsoft metadata warning. These are not Unity execution claims.
- Added standalone-only `Runtime/Episode/PortVerification.Blocs.cs` and hooks in the two earlier verifier partials. Explicit `--gamesim-verify-blocs` requires season verification and an absolute isolated save root; it is inert in the Editor and absent from normal gameplay. It observes actual newly created UI seasons, checking source NPC targets/public reasons, unchanged committed ballots, detached planning/RNG, reveal boundaries and marker reloads. Eligible pact rounds/changed choices/tie-breaks are reported only when naturally reached, not fabricated or required from a random season. The deterministic legal-pact tests separately own the changed-choice regression.
- Task launcher now targets `Builds/Port-Windows-V5/Gamesim.exe`, verifies explicit top-level/nested bloc report gates, and accepts copied schema3 or4 QA saves for complete-state migration checks (schema5/capturedWeek+1, preparation0 only for schema3). Review caught that default JObject parsing can coerce ISO-looking prose into equal Date values; the launcher now disables date parsing and rejects duplicate keys/trailing JSON. Distinct-prose/duplicate-key/syntax checks pass. Strictly rechecking accepted V4 migration with this reader still passes; immutable V4 artifacts were not modified. `Editor/U01ProjectSetup.cs` now builds separately to V5/report `Logs/Port-v5-build-report.json`.
- Corrected newly emitted competition/winner notification grammar to neutral “Competition winner: NAME”/“Gamesim winner: NAME” wording in engine and preparation paths. This resolves the visible “You wins” wording without changing arithmetic or rewriting old saved event text. Fixtures/provenance, roadmap and this change record were updated. No scene, material, NavMesh, package, source-control configuration, normal save, credential, remote, PR or backend changes were requested or made.
- Before integration, all byte baselines/staged hashes and new-file absence checks passed; Editor was idle and HousePrototype clean with17 roots. After apply, all20 core/persistence targets and three verifier sources matched their staged content (newline-normalized), with exact fixture bytes retained. The first comparison script misclassified the lowercase persistence manifest because of a case-insensitive filename check; its result was discarded and the corrected strict/error-stopping check passed. Unity refreshed/generated metadata; actual Edit/Play/build/standalone acceptance is recorded below when completed.

### Schema5 execution evidence — September13

- First Unity Edit Mode: **558 passed, zero failed/skipped**, run `797e46ee45f4422aa1c022307afc22b7`, job `be0e50c5-b566-42b2-a5b5-ca4541f41972`, 17:45:11–17:50:49 UTC,331.919 seconds. Retained `Logs/Port-v5-first-editmode-20260913.xml`/`.txt`. All133 non-meta Gamesim assets have valid unique metadata (88C# files); no missing/blank/duplicate GUIDs.
- This full run exposed migration cost rather than a failing rule: the16-phase history sweep took148.345 seconds, old-week/reload test19.669 seconds, and individual historical load cases roughly4.6–5.4 seconds. Read-only progress confirmed actual NUnit execution; no Editor restart/termination was performed. Repeated new JSON contract resolvers, amplified by the two frozen/current validation materializations on historical upgrades, are the targeted cost. [Json.NET's official performance guidance](https://www.newtonsoft.com/json/help/html/Performance.htm) recommends reusing contract resolvers while avoiding repeated reflective contract construction. A metadata-cache improvement and regression rerun are in progress; validation is not being weakened to make the tests faster.
- Task `work/test-unity-port-verify.ps1` passes12 non-player launcher checks: syntax, literal ISO-looking prose distinction, duplicate/nested duplicate/trailing/non-object rejection, detached schema3/4 expected-state construction preserving stored preparation, and required-season flag rejection before player launch/QA-directory creation. It does not substitute for executable verification.
- Cached only the fixed public-field contract resolver in `EpisodeSaveStore.cs`; every operation still constructs independent serializer/settings instances with unchanged strict options. The migration test capture helper also reuses its fixed resolver. Added three `PersistenceSerializerCacheTests.cs` regressions for shared contracts/distinct serializers, per-instance option isolation/unknown-field rejection, and detached nested state with literal ISO-looking prose/null/default/zero-RNG preservation. The first new fixture incorrectly indexed an appended memory as entry0 despite the catalog's arrival memory; corrected the test to use the captured append index. Focused managed checks then passed10/0; production behavior was not changed for that fixture correction.
- Post-cache full Unity Edit Mode: **561 passed, zero failed/skipped**, run `2dbca617da89412b9f95f9a4bb2cc045`, job `2ba774e3-37c0-44c8-a91e-ca934058de32`,18:00:55–18:01:13 UTC,14.296 seconds. Retained `Logs/Port-v5-cache-editmode-20260913.xml`/`.txt`. The same16-phase migration case now takes0.462323 seconds versus148.345024 in the first run. These are observed suite timings on this Editor, not a controlled benchmark or weaker validation gate. The next complete Play Mode/build/player runs exercise the optimized runtime.
- Complete Unity Play Mode: **47 passed, zero failed/skipped**, run `529a98d0480744f7ba0bbd76636547a0`, job `139a6a69-94b1-4945-b100-db09296ac43e`,18:01:47–18:03:35 UTC,102.129 seconds. Retained `Logs/Port-v5-playmode-20260913.xml`/`.txt`. The two new tests are methods on the existing partial `EpisodePlayModeTests` class: `VotingBloc_ActualPactAndCeremoniesChangeMayaBallotWithoutExposingCoordination` (3.347831seconds) and `VotingBloc_PrivateBallotDraftCancelReplacementAndActualReloadCannotReuseOldConfirmation` (1.873124seconds); both executed and passed. Navigation acceleration in these automated tests is not a human pacing claim. Read-only MCP afterward found Editor idle and HousePrototype loaded/clean with17 roots.
- Strict clean-cache V5 Windows build: **Succeeded, zero errors,497 package warnings and no Gamesim-owned warnings**,144,038,659bytes,76.5207412seconds. `Builds/Port-Windows-V5/Gamesim.exe`; `Logs/Port-v5-build-report.json`. Post-build read-only Console query returned zero errors; earlier transient Editor-toolbar errors remain recorded, not claimed repaired. Unity's test framework generated its two temporary PerformanceTestRun resource files during testing/build and removed them afterward; no manual asset deletion was performed.
- Post-build source audit against immutableV4:12 new non-meta Assets files with12 metadata files,16 modified Assets files plus the two port documents,zero deletions,zero changed old metadata. All134 non-meta Gamesim assets (89C#) have metadata with no missing/blank/duplicate GUIDs. Scenes/materials/NavMesh, Packages, ProjectSettings and source-control configuration are byte-identical to acceptedV4. Reusable read-only task audit: `work/compare-unity-port-source.ps1`.
- Isolated headless V5 study/bloc/full-season run **Passed, exit0**, completed18:07:39 UTC: `Logs/Port-verify-20260913-180618-9c10619ed11844bea3a05ee595330bd3`. Separate60.558-second season,55 decisions,6 save/reload checkpoints,5 study confirmations,3 weekly simulated competitions,preparation5 retained,one diary reflection and player-juror finish. Source comparisons checked3 regular NPC round plans and6 NPC ballots with6 marker reloads; all mandatory privacy/detachment gates passed. This random seed did not retain an eligible pact, change a coordinated target, reach an NPC tie-break/pending-ballot step, or declare an oath. The deterministic legal-pact Play Mode tests, not this run, certify changed-choice/private-confirmation coverage. Runtime errors are empty. Headless frame timings are not graphical performance evidence.
- That run also passes complete-state schema4→5 migration from a copied acceptedV4 QA profile save, original `Logs/Port-verify-20260913-171909-f09fde5191bf48b590e17e4159f6230a/episode.json`, SHA256 `D02ABE0F61C81FF09408F2C26D0E9E56F4958FAAEA81F2B89EE455A624F93D44`. Original bytes remain unchanged; the copy equals the entire original state except schema5 and explicit bloc activation at capturedWeek+1. No preparation or existing history is reset.
- First graphical V5 run began18:08:03 UTC under `Logs/Port-verify-20260913-180803-06a0c7f8721e4af6b2d86454ff7f9c70`. An unrelated staged next-increment helper compile overlapped its sampling window (DLL timestamp18:09:08.0045863 UTC, command9.445seconds; exact completion time not captured). Its functional results remain valid if they pass, but timing will be retained with this contamination note and followed by a separate profile-only run without concurrent compilation/generation. No live Assets changed during either workload.
- That graphical run **Passed, exit0**, including the separate110.8178467-second actual-UI season (seed2889133207), completed18:15:02 UTC:62 decisions,9 save/reload/marker checks,3 diary reflections,5 study confirmations,5 weekly simulations,2 assisted final-HoH decisions,4 player-finalist answers and a submitted speech. Player finished RunnerUp. Bloc checks covered3 plans/6 NPC ballots,1 preserved prior ballot,1 pending privacy step,3 reveal checks and1 NPC HoH tie-break; no eligible pact/changed choice or oath was naturally reached. All runtime error arrays are empty and profile save bytes were preserved. Complete copiedV4 migration/source preservation also passed. Its300.0277614-second/264,444-frame profile is retained only with the compile-overlap qualification above; the clean repeat owns final timing evidence.
- Inspected the first V5 graphical house, larger-text1280x720 Settings, weekly simulation result, player-finalist questioning and finale captures. Layouts are readable and scroll where needed. Newly emitted competition/winner status grammar is corrected; older saved messages are untouched. This visual inspection is not human playtesting or final-art acceptance.
- Clean repeat graphical profile **Passed, exit0**, completed18:20:38 UTC: `Logs/Port-verify-20260913-181532-641830f303a149ca8517aefc70f44203`. No concurrent staged compilation/generation during these samples.300.027952seconds,267,302frames,1600x900, GTX1060 6GB/i5-8400; median1.02819586ms,p951.69469953ms,p992.30869460ms,empty runtime errors. Allocation counter unavailable; no zero-allocation claim. This is the automated five-room/navigation/panels/save workload, not an isolated hardware benchmark or human pacing. This repeat did not request another functional season; the preceding same-binary season report owns that coverage.
- The clean repeat additionally passes full-state copied-schema3→5 migration: original acceptedV3 QA save SHA256 `EE6D309809DEEE2D7817C8C0D716D108D60CA8EE4F66F9F9A85DCDE56ACA19B1` remains unchanged, and the copy differs only by schema5, preparation0 and bloc activation at capturedWeek+1. Both V5 graphical build manifests were independently rehashed:215 files each,zero mismatches or extra/missing files. AcceptedV4's215 build hashes also remain unchanged. Read-only audit afterward found Editor idle and HousePrototype loaded/clean with17 roots.
- Immutable acceptedV5 source/settings snapshot: task `work/unity-v5-source-20260913`; matching launcher `work/unity-v5-verify-20260913.ps1`; exact acceptance index `work/unity-v5-acceptance-20260913.json`. Future NPC conversation rules/house-motion/persistence work is staged separately under `work/unity-port-next/autonomy` and is not part of this binary or source pin. Every material V5 source change is enumerated above; normal saves, credentials, accounts, backends, Git remotes and PRs were not changed.

The six-person format deliberately makes all four evictees jurors; it is not the original larger-cast week-five jury schedule. Regular nominations/replacements use the source offline relationship ordering and veto legality; the scenario has no target-agreement deals. Final NPC selection uses the source fast-forward per-voter policy, not the richer live NPC decision engine. Jury NPC ballots use directed trust plus two persisted ±10 rolls; ties follow the source's second-finalist cast-order rule. UI-delay randomness is excluded from gameplay replay.

The native precision challenge supplies a bounded new input to source competition arithmetic; Final HoH Part1 applies it to temporary effective endurance. Stored stats do not change. Fixed cast stats/starting relationships, social-window budgets, promise lifecycle and new private memories are scenario policies, not claims of whole-web-game equivalence. The per-voter helper receives actual stored relationships, arcs and persona. Schema5's eligible regular-round wrapper derives source bloc pressure from real pact membership; it does not fabricate unstored deals, founder/stability or grudges.

Web import supports only receipt-free six-active-cast social snapshots within the declared importer contract. Raw exports are archived before interpretation. Complex in-progress saves, unsupported systems, extra fields, and recovery wrappers are rejected with a reason; no general migration is claimed. Local checksum detects corruption, not malicious tampering. Local save JSON is not encrypted. No access/refresh/API tokens are stored or requested by this offline implementation.

## Still open before complete original-game / production acceptance

### September13 V6 integration — automated acceptance passed within native scope

The reviewed NPC conversation increment passed the separate Unity/build/desktop gates recorded below. All375 accepted V5 source/config/doc files were SHA-checked unchanged before integration; Unity was idle on the clean17-root HousePrototype scene with zero current Console errors. The source batch was bracketed with AssetDatabase.StartAssetEditing/StopAssetEditing and refreshed. Existing scene/material/NavMesh assets, Packages, ProjectSettings, configuration, old metadata, earlier builds and ordinary player saves remain untouched. Unity's existing MainToolbarWindow/UIElements layout-stabilization errors recurred during reload; they are not suppressed or described as fixed.

Material source inventory versus V5 after regression additions:22 existing C# files changed,22 new C# files and one new original-source fixture added, each with Unity-generated metadata; no deletions. Changes are:

- Simulation: new WebNpcConversations, EpisodeNpcSocial, NpcSocialState and EpisodeNpcSocialValidation; EpisodeEngine adds detached trusted operations and injects the independent reciprocal roll through the original relationship reducer; EpisodeState/Validation/ContentCatalog add current schema6 initialization/guards.
- Persistence: frozen recursive V5 DTO/validator additions; migration/store/current validation/importer changes add only the seeded NPC subsystem and schemaVersion to old captures. Fresh games activate at week1, all older/imported snapshots at capturedWeek+1. Existing bloc activation, main RNG, history and pending ballots are retained. Old v4→5 validation is frozen rather than delegated to an incompatible current validator.
- House: HouseRoomQuery, HouseNpcMotion and HouseMeetingCoordinator additions use the existing floors/NavMesh/collision shapes, exclusive paired reservations, complete paths, physical arrival and sight/occupancy checks. Four fixed venues exclude Private; bedroom slots at z3.5 avoid the unchanged Riley spawn and the prototype's protected Bedroom marker. Initial binding adjusts at most.25m; no travel teleport/rebake. Loading resets actors to their captured authored starting placements and walks saved pending pairs back to the same venue without rerolling.
- Director: new Durable/NpcSocial partials and generic HouseConversationCaption; existing Director/DiaryRoom changes share prepare→save→publish for player/NPC operations, pause for panels, reject stale load-generation work and old-session callbacks, stop on failed saves, and lock movement/camera for ambiguous recovery. Load/recovery/new/import never flush the old slot before reading/replacing it. Aggregate relationship arcs are no longer shown as player notebook narratives because they lack participant/knowledge provenance; player-owned trust, memories, promises and audience-filtered events remain. Finale prose now names winner/runner-up neutrally.
- Verification: new source-leaf and engine Edit tests,55 schema6 persistence cases,14 house primitive/pair Play tests,9 durable and9 runtime Play regressions, one actual-completion save failure/retry test and four saved scan-cadence regressions; existing historical migration/preparation/bloc tests adjust only current field accounting. Two existing Play test sources explicitly account for NPC-only free-walking effects while retaining full modal-state guards and exact finale wording. New opt-in standalone autonomy workload plus existing profile/season/study integration changes capture player decisions after modal pause, not before free-roam NPC time. The build menu now targets separate V6 output/report paths.

The original hook source SHA256 is0DB24163097217BC6E0458C6D5911C82F8F31AB6BD44ADD2DFAF97760AFA5528. Its2804-row extracted fixture is1,715,017 bytes, SHA2569B3A45468E81DDE36E6231A5B8CF7ACBE96BDF42D3155DF259BAF02CA0805106, copied unchanged. Staged combined simulation/persistence checks rerun637 passed/0 failed; metadata compilation covers Editor and standalone Runtime plus all Edit/Play sources. These are not Unity native execution or shipping acceptance. Exact staging handoffs, authority distinctions and source manifests remain under the task's work/unity-port-next/autonomy directory.

Native policy is explicit: active-player Social/Campaign only, whole committed seconds, no offline catch-up, three-second approach scans, pre-arrival approach leases transient, durable memory/topic/duration only at accepted physical start, completion from current source context, separate seeded RNG. Persisted native traits/active pacts/directed scores replace the original main route's name-template/missing-alliance/stale-map quirks. Pending reunion pauses logical time; failed arrived-pair obstruction cancels after20 eligible real seconds without completion effects. Unknown/changed geometry stays suspended for recovery. Generic nearby same-room/LOS captions expose no gossip targets, private outcomes or pact proof. This is not the full original strategic/motive/furniture/player-approach planner.

Initial Unity acceptance evidence is retained, including failures: Edit Mode640/0/0 (`e29e047fa7bb4e26a670404718ea6668`,19.008s); first Play63/16/0 (`744f8526ae554f8885d26c70f6ab8f6b`,136.430s); second Play77/3/0 (`36a92d524a624863964cf159dccfbc06`,111.058s). Logs are separately copied as Port-v6-first-editmode, Port-v6-first-playmode and Port-v6-second-playmode with the20260913 suffix. The first14 failures shared a test-fixture lifecycle error: disabling HouseInteraction destroyed its HUD but did not stop its Start coroutine. The fixture now deactivates its separate controller object, with guards preserving actual player/NPC/camera roots. Two old assertions now explicitly allow only source-owned NPC changes during free walking, require full modal-state stability, and check the neutral finale text. No production log was suppressed.

The second run passed the real arrived-completion failure/retry regression and all existing episode/durable/runtime tests. Three remaining house failures drove precise fixes: preserve the1.25m marker clearance by moving both bedroom slots to z3.5; place the far-observer fixture away from the original planter; and remove actual paused movement drift without changing its.03m test tolerance. Paused motion now writes zero agent velocity each frame while retaining its stopped path, consistent with [Unity's velocity override semantics](https://docs.unity3d.com/ScriptReference/AI.NavMeshAgent-velocity.html). Native scene geometry/NavMesh remain untouched.

Post-review cadence correction: new route planning now consumes only a successfully saved/installed Tick result's scanDue at the persisted nextScanTick, not an independent reset-on-load timer. Arrival-triggered starts remain separate. A reload/crash discards only unsaved approach routes and waits for the next committed deadline; it never rerolls a saved conversation. Existing captions are hidden per frame when current physical witness proof expires; new topics are selected/shown at10Hz. Standalone QA captures every frame across screenshot waits and requires independent room/range/LOS, actual post-load reunion proof, a frozen unready interval, and sufficient pending duration to observe arrival before completion. The V6 launcher and its actual-function tests pass134 checks, including schema3/4/5 migration expectations and mandatory physical-proof report gates. Full corrected Unity/build/standalone acceptance remains pending.

Third Play Mode run:83 passed/1 failed/0 skipped, run187db603b5ff4232a276a341531d87d3,120.831 seconds; retained Port-v6-third-playmode-20260913 reports. All four cadence cases, both meeting fixes and the strict paused-motion check passed. The remaining movement assertion was after release: the native ResetPath call left isStopped false when stop was set before clearing the path. Release now clears the path first, then establishes stopped/zero-velocity ownership. Idle bound agents also receive zero velocity each frame. The regression additionally waits eight frames and requires no path and less than.03m displacement; no tolerance was weakened.

Corrected Unity Play Mode suite **84 passed, zero failed/skipped**, run a1c11a52b7af4bdea133ae8682eafb80, job c9b4af4d-2746-4041-b08b-45b1737e508c,19:26:36–19:28:44 UTC,122.339 seconds. Exact reports retained as Logs/Port-v6-final-playmode-20260913.xml/.txt. This is actual Unity navigation/UI/persistence execution, not the staged metadata compile. The build menu now also rejects a compiling/updating/playing Editor before touching build settings; its prior clean-scene guard remains. Final Edit Mode/build/standalone verification follows separately.

Final Unity Edit Mode **640 passed, zero failed/skipped**, run a35edf940808447cb196859310db9f3b, job5313e585-2da7-48dc-9b73-eea5e0895014,19:29:35–19:29:59 UTC,19.167 seconds; Logs/Port-v6-final-editmode-20260913.xml/.txt. Read-only audit found HousePrototype clean/loaded with17 roots. All157 non-meta Gamesim assets/111 C# files have unique valid metadata; zero deletions, old-metadata changes, scene/material/NavMesh/package/settings/config changes versus immutableV5. Unity removed its own temporary test scene after completion. The pre-build Console query again showed editor-toolbar layout-stabilization errors only in its eight returned error entries; no claim is made that those editor errors are repaired.

Strict clean-cache V6 Windows build **Succeeded, zero errors,497 package warnings, no Gamesim-owned warnings**,144,167,539 bytes,77.7198525 seconds. Separate output Builds/Port-Windows-V6/Gamesim.exe and Logs/Port-v6-build-report.json preserve every earlier accepted build. The first isolated standalone run begins with an unchanged copy of the accepted V5 QA profile save; its original SHA256 is4808D5AE62FCACE0D639F8A99AF47590FB86007F04CE2B620148FE32EA559483. Executable verification and graphical performance evidence are not inferred from build success.

First isolated headless V6 run **Passed, exit0**, completed19:33:49 UTC: Logs/Port-verify-20260913-193159-d11b6394e1b34114b4f958b068f42a23. Actual90.1128853-second UI season:55 decisions,7 save/reload checks,one diary reflection,5 study confirmations,3 weekly simulations,preparation5 retained,player-juror finish. Source bloc checks covered3 plans/6 NPC ballots and7 marker reloads; eligible coordinated pact/changed choice/pending-ballot/NPC-tie/oath branches were not naturally reached. Deterministic Play tests own those legal-pact/private-draft cases. Runtime error arrays are empty.

The required autonomous-conversation workload also passed:3 actual-start proof checks,24 complete detached source-transition comparisons,1 completed conversation,1 loaded-pair reunion proof,12,353 unready frozen frames and14,263 independently checked caption frames. Real modal motion/full-state pause, exact pending reload and unchanged player RNG/actions/receipts/events/memories all passed. Copied schema5→6 migration preserves the entire original state except schema6 and the explicitly initialized next-week NPC subsystem; original source bytes remain unchanged. Ten-second headless frame timings are not graphical evidence. The following clean five-minute graphical run uses a fresh native season rather than a migrated old-week save, so the profile includes new NPC activity instead of deferring it through the historical activation guard.

Clean graphical V6 run **Passed, exit0**, Logs/Port-verify-20260913-193536-7a0b9ebd68ae485bb28362d261a29f1d. Profile finished19:40:42.0302911 UTC:300.0297424 seconds,211,388 frames,1600x900 on GTX1060 6GB/i5-8400,median1.310197ms,p951.902997ms,p992.410898ms. No staged compilation/generation overlapped the profile; all workers acknowledged the freeze before launch and were released only after the post-workload capture/new-season slot. Allocation counter unavailable, not zero. The profile's native week1 save advanced to227 committed social seconds and next issued sequence17, so this measured actual new NPC activity as well as five-room travel, panels and saves. These are automated frame intervals, not isolated GPU measurements, broad hardware certification or human pacing.

The same graphical executable then passed its separate86.5762128-second actual-UI season, seed3850228356, completed19:42:08.6693685 UTC:55 decisions,7 reload checks,one diary reflection,5 study confirmations,3 weekly simulations,preparation5 retained,player-juror finish. All required study/bloc/autonomy gates passed, profile slot bytes were preserved, and runtime errors are empty. Autonomy checked3 actual-start proofs,19 entire source transitions,1 completed conversation,1 loaded reunion,3,212 unready frozen frames and4,111 physical caption proofs. Optional eligible-pact/changed-choice/tie/pending-vote/assisted-final-HoH/speech/oath branches were not naturally reached; their absence is not claimed as runtime coverage. Reviewed the generic witnessed-gossip caption, larger1280x720 Settings and neutral finale captures; current procedural visuals are not final-art approval.

A separate copied-schema4→6 headless migration smoke **Passed, exit0**, finished19:42:57 UTC: Logs/Port-verify-20260913-194245-a6ebd8f1cf8d43b0839d63245db98b68. Original acceptedV4 QA save SHA256 D02ABE0F61C81FF09408F2C26D0E9E56F4958FAAEA81F2B89EE455A624F93D44 remains unchanged. The complete state is retained with only schema6, missing historical bloc activation and the new next-week seeded NPC subsystem added; stored preparation and history are not reset. This smoke requested no second season; the preceding same-binary runs own functional coverage.

Accepted V6 source/config/doc snapshot: task work/unity-v6-source-20260913; matching launcher and its134-check harness retained as work/unity-v6-verify-20260913.ps1 and work/unity-v6-launcher-tests-20260913.ps1. Acceptance index: work/unity-v6-acceptance-20260913.json. The new motive/furniture rule packages under work/unity-port-next/motives are separate staging work, not part of this accepted binary. Every material V6 source change and failed-then-corrected native test is recorded above. No normal save, credential, account, backend, remote, commit or PR was changed.

### Post-V6 needs/activity-rule prerequisite — integrated, Edit Mode passed

The421-file/9,834,202-byte V6 source snapshot was compared back to live with zero differences before this increment. Added three pure Simulation sources (WebNpcMotives, WebNpcActivityRules, WebNpcActivityCatalog), two Edit test sources and two unchanged source-generated fixtures. No runtime consumer, save version, need meter, new scene or furniture interaction is added by these leaves. The build menu now targets separate Builds/Port-Windows-V7/Gamesim.exe and Logs/Port-v7-build-report.json so a later build cannot overwrite accepted V6.

The motive fixture is1,771,920 bytes/SHA256 F3228526A3F8DBBCB442F8A9243492F2B61DA96B2304BD66DAAFBB0720ACE6E8,3,623 source rows; staged installed-reference checks passed16. The activity fixture is932,036 bytes/SHA256 3E1997F03F97B69EF0A3B6A55C55D6F4219D804566E07173F3F28C17B6558CAB,1,091 rows plus61 point/12 phase/8 chain/2 carry metadata records; staged checks passed17 after duplicate/null-ID guards were added for lists representing source Maps. Both generators ran twice byte-identically from guarded unchanged TypeScript; full handoffs/manifests/generators remain in task work/unity-port-next/motives. Catalogs omit source world coordinates and return detached data. No new assembly/package dependency is required.

Preserved quirks include ordered duplicate-trait effects, lower-only decay, nonpositive satisfaction ignored, strict threshold boundaries, emergency priority, source in-place stable top-three sorting, all supplied draw counts, unreachable player-persona scoring in the actual seek loop, and first-match chains that shadow later templates. Finite typed-input guards are native boundaries, not arbitrary malformed-JavaScript equivalence. Only transcendental Math.Pow-derived numeric comparisons have explicit tiny cross-runtime tolerances; decisions/order/draws are checked exactly.

Separate actual React18 caller characterization covers six controlled scenarios in baseline development, production with the source StrictMode wrapper, and development StrictMode. Repeated evidence hashes are recorded in motives/caller/RENDERER_RESULTS.md. Production confirms delayed completion-updater draw order; development StrictMode replays draws/timers/events without doubling final need state. These are isolated jsdom/fake-clock observations, not browser movement tests or a universal source-seed trajectory. A future native furniture scheduler must document its own single durable completion and clock/needs policy.

Integration correction: the motive package's relative new-file paths initially landed in the task workspace; their exact staged contents were verified, added at the intended absolute Unity paths, and only those two newly created misplaced copies were removed. No pre-existing project or workspace file was overwritten/deleted. Both fixture byte hashes match in Unity. Further one-TV-routine domain/persistence/world work remains staged separately; it is not covered by V6 acceptance or made playable by adding these helpers.

Actual Unity Edit Mode after both leaf packages: **673 passed, zero failed/skipped**, run0695846a5240402dbba86a99739be5d3, jobf4ea4957-394f-4dfd-a487-bd23c351c584,19:50:59–19:51:23 UTC,20.566 seconds. Retained Logs/Port-activity-leaves-editmode-20260913.xml/.txt. This includes33 new grouped/parameterized checks and the entire previous640-test suite. Added fixture README provenance for the accepted conversation leaf and the two new packages; the historical accepted V6 snapshot is not rewritten. No V7 executable has been built or accepted.

Post-leaf Unity Play Mode regression **84 passed, zero failed/skipped**, runfea0e4f107c845dc9164a16b1b2c5132, jobcde233e0-0cd6-4f81-af60-7fe05e1f30e0,19:52:32–19:55:22 UTC,163.863 seconds; Logs/Port-activity-leaves-playmode-20260913.xml/.txt. The live project therefore passes both suites with the unused pure rules present. Further furniture domain/state/world/director and visual-child-facing code is staging only under task work/unity-port-next/activity-native; it has not been installed, built or natively accepted.

1. Broader original systems: deals/dynamic alliance founding-stability/grudges/full milestones, autonomous NPC social schedules, storylines/house events/full diary scheduling/crises/war-room systems, full mental-state/weekly-focus modifiers, full competition/minigame catalogue and general full-cast/multi-phase save migration. Source arcs and jury questioning/final speeches passed schema-2 acceptance. Schema3 adds post-eviction diary/persona, an impression ledger and player oath declaration/breach paths; schema4 adds private-room Social study and the weekly weighted simulation consumer; schema5 adds private voting-bloc coordination from actual pact membership. These increments do not imply that every related web subsystem or source activity schedule is ported.
2. Optional account/cloud/AI adapters and their expiry/offline/reconnect verification. No backend has been connected; local gameplay is independent of it.
3. Final art/animation/audio and staged ceremonies/departures. Current visual assets are procedural and require approval against a defined production bar.
4. First-time human playtests, 30–45-minute pacing acceptance and target-hardware coverage beyond this machine. Automated tests cannot sign these off.

This record intentionally separates a playable local slice/season from a complete feature-equivalent original game or an approved AAA-quality release.
